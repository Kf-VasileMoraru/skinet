name: skinetAPi

on:
  workflow_dispatch:
#  push: 
  
jobs:
  build:
    runs-on: self-hosted
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: test location
        run: pwd
        
      - name: copy file via ssh key
        uses: appleboy/scp-action@v0.1.4
        with:
          host: 10.10.11.101
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
#          rm: true
          source: "dockerDev/docker-compose.yml"
          target: /skinet
        
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v3
#      
#      - name: Copy appsettings.json
#        run: cp ./dockerDev/appsettings.json ./dockerDev/appsettings.Development.json
#      
#      - name: Login to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKERHUB_USERNAME }}
#          password: ${{ secrets.DOCKERHUB_TOKEN }}
#      
#      - name: Build and push
#        uses: docker/build-push-action@v5
#        with:
#          context: ./dockerDev
#          push: true
#          tags: |
#            a2259687814/skinetApi:latest
#            a2259687814/skinetApi:${{ github.sha }}
#          cache-from: type=registry,ref=a2259687814/skinetApi:buildcache
#          cache-to: type=registry,ref=a2259687814/skinetApi:buildcache,mode=max
#          
#
#      - name: Delay for 30s
#        run: sleep 30  
        
#      - name: Set up SSH
#        uses: appleboy/ssh-action@master
#        with:
#          host: 10.10.11.101
#          username: root
#          key: ${{ secrets.SSH_PRIVATE_KEY }}
#          script: |
#            cd /skinet/dockerDev
#            docker stop skinetAPI
#            docker rm skinetAPI
#            docker compose -p skinetAPI up -d
      
